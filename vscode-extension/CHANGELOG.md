# Changelog

All notable changes to this extension are documented in this file.

## [0.1.0] - Unreleased

### Added

- Initial release: `Morpheus: Initialize` command and auto-run-on-first-activation, both invoking the bundled `install.sh` to install the CLI to `~/.morpheus` and deploy the Cyber Sierra compliance skill to `~/.cursor/skills/cyber-sierra` and `~/.claude/skills/cyber-sierra`.
